我对关于 '异常控制流' 的理解 (是指语言抽象上的异常控制流, 而不是硬件/操作系统层面的异常控制流, 尽管概念相似)

讨论通常编程语言中的正常/异常控制流, 并没有涵盖一些设计思路很特殊的语言

正常控制流
-------------------------------------------------------------------------------
显式由某个语法所表明的控制流, 像是

- `if` `while` 常见的控制流
- `try` `catch` 也是显式的, 正常的控制流
- `finally` `defer` 尽管作用位置有些不同, 但也是显式的控制流

而有些控制流是向外层而不是向内发生的, 像是

- `break` `continue` `return` 这些也是正常的控制流
- rust 的 `expr?`, zig 的 `try expr` 由显式的语法让函数可能返回
- `throw` 显式, 仅从这个语句所在函数局部来看, 是正常的控制流

异常控制流
-------------------------------------------------------------------------------
隐式的, 向外传播的控制流, 像是隐式的函数返回, 隐式的抛出异常

- 调用的内层函数或某些运算抛出了异常 (像 `throw`), 这在语法层面是较为隐式的
- 某些语言的 panic, 不断的弹出调用栈逐层展开
- c 的内层函数发生 `longjmp`, 产生的控制流甚至都不一定称之为控制流了
- 终止、进程退出等, 也是异常控制流
- 信号异常, 进程接收到信号强行将控制流切换到信号处理程序中,
  这也是和操作系统层面的异常控制流有交集的地方

> [!NOTE]
> 关于操作系统层面的'异常控制流', 系统中断切换上下文这类, 并不认为是语言层面的控制流,
> 因为语言对这类操作基本上是无感知的, 也没有影响运行顺序
